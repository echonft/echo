{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "artifacts:delete": {
      "cache": false
    },
    "deploy": {
      "dependsOn": ["lint", "tsc", "build"],
      "cache": false
    }
  }
}
