{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "deploy": {
      "dependsOn": ["lint", "tsc", "build"],
      "cache": false,
      "passThroughEnv": ["CI", "ENV"]
    }
  }
}
