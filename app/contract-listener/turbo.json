{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["dist/**"]
    },
    "machine:restart:dev": {
      "cache": false
    },
    "machine:restart:prod": {
      "cache": false
    }
  }
}
