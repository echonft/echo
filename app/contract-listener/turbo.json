{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["src/**/*.ts"],
      "outputs": ["dist/**"]
    },
    "deploy:dev": {
      "cache": false
    },
    "deploy:prod": {
      "cache": false
    }
  }
}
