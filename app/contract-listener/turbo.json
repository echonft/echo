{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["dist/**"]
    },
    "deploy": {
      "cache": false,
      "passThroughEnv": ["ENV"]
    },
    "machine:restart": {
      "cache": false,
      "passThroughEnv": ["ENV"]
    }
  }
}
