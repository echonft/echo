{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["storybook-static/**"]
    },
    "dev": {
      "dependsOn": ["@echo/ui#build"]
    }
  }
}
