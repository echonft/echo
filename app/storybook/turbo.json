{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["storybook-static/**"]
    },
    "dev": {
      "dependsOn": ["@echo/ui#build"],
      "passThroughEnv": ["ENV", "LOG_LEVEL", "NODE_ENV"],
      "cache": false,
      "persistent": true
    }
  }
}
