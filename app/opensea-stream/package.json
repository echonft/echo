{
  "name": "@echo/opensea-stream",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "source .env.keys; dotenvx run --env-file=.env.dev -- tsup",
    "build:ci": "tsup",
    "clean": "rm -rf dist",
    "dev": "source .env.keys; nodemon --watch \"src/**\" --watch \"../../lib/**/src/**\" --ext \"ts\" --exec \"dotenvx run --env-file=.env.dev -- ts-node --project tsconfig.json src/index.ts\"",
    "lint": "eslint --fix -c .eslintrc.js --ext .ts .",
    "lint:ci": "eslint --quiet -c .eslintrc.js --ext .ts .",
    "lint:clean-cache": "rm -f .eslintcache",
    "modules:remove": "rm -rf node_modules",
    "modules:update": "ncu -u",
    "modules:update-minor": "ncu -u --target=minor",
    "prettier": "prettier -u --no-error-on-unmatched-pattern --write --cache .",
    "prettier:ci": "prettier -u --no-error-on-unmatched-pattern --log-level=silent -c .",
    "start": "node dist/index.js",
    "test": "jest --passWithNoTests",
    "test:ci": "jest --ci --no-cache --passWithNoTests",
    "test:watch": "jest --watch --passWithNoTests",
    "tsc": "tsc --noEmit",
    "tsc:ci": "pnpm run tsc",
    "tsc:clean-cache": "rm -f tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@opensea/stream-js": "0.1.3",
    "ramda": "0.30.0",
    "ws": "8.17.0"
  },
  "devDependencies": {
    "@babel/core": "7.24.6",
    "@babel/preset-env": "7.24.6",
    "@babel/preset-typescript": "7.24.6",
    "@dotenvx/dotenvx": "0.44.0",
    "@echo/firestore": "workspace:*",
    "@echo/model": "workspace:*",
    "@echo/opensea": "workspace:*",
    "@echo/utils": "workspace:*",
    "@echo/web3": "workspace:*",
    "@jest/globals": "29.7.0",
    "@jest/types": "29.6.3",
    "@types/node": "20.12.12",
    "@types/ramda": "0.30.0",
    "@types/ws": "8.5.10",
    "jest": "29.7.0",
    "jest-junit": "16.0.0",
    "nodemon": "3.1.1",
    "ts-node": "10.9.2",
    "tsconfig-paths": "4.2.0",
    "tsup": "8.0.2",
    "typescript": "5.4.5"
  }
}
