{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["src/**/*.ts"],
      "outputs": ["dist/**"]
    },
    "deploy": {},
    "deploy:dev": {},
    "deploy:prod": {},
    "machine:restart": {},
    "machine:restart:dev": {},
    "machine:restart:prod": {}
  }
}
