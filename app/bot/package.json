{
  "name": "@echo/bot",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "ci:lint": "eslint \"src/**/*.ts\"",
    "ci:prettier": "prettier \"src/**/*.ts\"",
    "ci:test": "jest --ci",
    "clean": "rm -rf dist",
    "deploy-commands": "node dist/scripts/deploy-commands.js",
    "dev": "tsup --watch",
    "lint": "eslint --cache --fix \"src/**/*.ts\"",
    "lint:clean-cache": "rm -f .eslintcache",
    "remove-modules": "rm -rf node_modules",
    "start": "node dist/index.mjs",
    "start:dev": "nodemon --watch 'dist/*' --watch 'node_modules/@echo/*' dist/index.mjs",
    "test": "jest --watch",
    "tsc": "tsc --noEmit",
    "tsc:clean-cache": "rm -f tsconfig.tsbuildinfo",
    "update-modules": "ncu -u"
  },
  "dependencies": {
    "@echo/api": "workspace:*",
    "@echo/discord": "workspace:*",
    "@echo/firebase-admin": "workspace:*",
    "@echo/model": "workspace:*",
    "@echo/utils": "workspace:*",
    "@mobily/ts-belt": "4.0.0-rc.5",
    "axios": "1.3.4",
    "discord.js": "14.8.0",
    "ramda": "0.28.0"
  },
  "devDependencies": {
    "@babel/core": "7.21.3",
    "@babel/preset-env": "7.20.2",
    "@types/ramda": "0.28.23",
    "nodemon": "2.0.21",
    "babel-jest": "29.4.2",
    "tsup": "6.7.0"
  }
}
