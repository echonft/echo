{
  "name": "@echo/bot",
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsup",
    "clean": "rimraf dist",
    "dev": "tsx watch src/index.ts | pino-pretty -c",
    "lint": "eslint",
    "modules:remove": "rimraf node_modules",
    "modules:update": "ncu -u",
    "modules:update-minor": "ncu -u --target=minor",
    "prettier": "prettier . -u -c --no-error-on-unmatched-pattern --ignore-path ../../.prettierignore --config ../../prettier.config.js",
    "sentry:sourcemaps:inject": "sentry-cli sourcemaps inject --org=echo-nft --project=bot dist/",
    "sentry:sourcemaps:upload": "sentry-cli sourcemaps upload --org=echo-nft --project=bot dist/",
    "start": "node dist/index.js",
    "test": "jest",
    "test:ci": "jest --ci --collectCoverage --reporters=default --reporters=jest-junit",
    "test:clean": "rimraf test/.coverage",
    "tsc": "tsc --noEmit"
  },
  "dependencies": {
    "@sentry/node": "8.39.0",
    "@sentry/profiling-node": "8.39.0",
    "discord.js": "14.16.3",
    "i18next": "23.16.6",
    "ramda": "0.30.1"
  },
  "devDependencies": {
    "@babel/core": "7.26.0",
    "@babel/preset-env": "7.26.0",
    "@babel/preset-typescript": "7.26.0",
    "@echo/firestore": "workspace:*",
    "@echo/model": "workspace:*",
    "@echo/routing": "workspace:*",
    "@echo/utils": "workspace:*",
    "@jest/globals": "29.7.0",
    "@jest/types": "29.6.3",
    "@sentry/cli": "2.38.2",
    "@types/node": "22.9.0",
    "@types/ramda": "0.30.2",
    "jest": "29.7.0",
    "jest-junit": "16.0.0",
    "tsup": "8.3.5",
    "tsx": "4.19.2",
    "typescript": "5.6.3"
  }
}
