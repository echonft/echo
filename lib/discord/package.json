{
  "name": "@echo/discord",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "lint": "eslint --cache --fix \"./**/*.ts\"",
    "prettier": "prettier --write \"./**/*.ts\"",
    "tsc": "tsc --noEmit --skipLibCheck"
  },
  "dependencies": {
    "ramda": "^0.28.0"
  }
}
