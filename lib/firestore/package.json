{
  "name": "@echo/firestore",
  "version": "1.0.0",
  "private": true,
  "sideEffects": false,
  "license": "MIT",
  "scripts": {
    "db:add-collection:dev": "ts-node --project tsconfig.json utils/add-collection-dev.ts",
    "db:fix-addresses:dev": "ts-node --project tsconfig.json utils/fix-addresses-dev.ts",
    "db:reset:dev": "ts-node --project tsconfig.json utils/reset-db-dev.ts",
    "db:reset:test": "ts-node --project tsconfig.json utils/reset-db-test.ts",
    "lint": "eslint --cache --fix --ext .ts src/ && eslint --cache --fix --ext .ts test/ && eslint --cache --fix --ext .ts utils/",
    "lint:ci": "eslint --quiet --ext .ts src/ && eslint --ext .ts test/ && eslint --ext .ts utils/",
    "lint:clean-cache": "rm -f .eslintcache",
    "modules:remove-all": "rm -rf node_modules",
    "modules:update-all": "ncu -u",
    "modules:upgrade": "pnpm upgrade",
    "prettier": "prettier -u -c src/ && prettier -u -c test/ && prettier -u -c utils/",
    "test": "pnpm run db:reset:test && jest --runInBand",
    "test:ci": "pnpm run db:reset:test && jest --runInBand --ci --no-cache",
    "test:watch": "jest --watch",
    "tsc": "tsc --noEmit",
    "tsc:ci": "tsc --noEmit",
    "tsc:clean-cache": "rm -f tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@auth/firebase-adapter": "1.0.3",
    "@echo/model": "workspace:*",
    "@echo/utils": "workspace:*",
    "dayjs": "1.11.10",
    "firebase-admin": "11.11.0",
    "ramda": "0.29.1"
  },
  "devDependencies": {
    "@jest/globals": "29.7.0",
    "@jest/types": "29.6.3",
    "@types/prompt-sync": "4.2.3",
    "@types/ramda": "0.29.7",
    "jest": "29.7.0",
    "jest-junit": "16.0.0",
    "prompt-sync": "4.2.0",
    "ts-node": "10.9.1",
    "tsconfig-paths": "4.2.0"
  }
}
