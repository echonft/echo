export interface AccountDocumentData {
  access_token: string
  expires_at: number
  provider: 'discord'
  providerAccountId: string
  refresh_token: string
  scope: string
  token_type: 'Bearer'
  type: 'oauth'
  userId: string
}
