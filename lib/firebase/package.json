{
  "name": "@echo/firebase",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "lint": "eslint --cache --fix \"./**/*.ts\"",
    "prettier": "prettier --config --write \"./**/*.ts\"",
    "tsc": "tsc --noEmit --skipLibCheck"
  },
  "dependencies": {
    "@echo/model": "1.0.0",
    "ethers": "^5.7.2",
    "firebase": "^9",
    "ramda": "^0.28.0"
  },
  "devDependencies": {
    "firebase-admin": "^11.2.1"
  }
}
