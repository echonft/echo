import { OfferState } from '@echo/ui-model'

// TODO Update function with the new offer state with swap
export const getOfferModalDeclineButtonTitleForState = (_state: OfferState) => {
  return 'declineBtn'
}
