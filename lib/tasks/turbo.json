{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "command": {
      "cache": false,
      "persistent": true,
      "passThroughEnv": ["ENV", "LOG_LEVEL", "NEXT_PUBLIC_VERCEL_PROJECT_PRODUCTION_URL"]
    },
    "firestore:migration": {
      "cache": false,
      "persistent": true,
      "passThroughEnv": ["ENV", "MIGRATION"]
    }
  }
}
