export enum ListingError {
  Duplicate = 'listing is a duplicate',
  Exists = 'listing already exists',
  Locked = 'listing is locked',
  NotFound = 'listing does not exist',
  PostExists = 'listing  post already exists'
}
